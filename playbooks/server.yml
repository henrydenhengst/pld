---
- name: Richt de PLD Server in
  hosts: all
  become: yes
  roles:
    - network       # Hardware check & IP 192.168.100.1
    - dhcp          # Deelt adressen uit in 192.168.100.x
    - apt_proxy     # Cache voor de pakketten
